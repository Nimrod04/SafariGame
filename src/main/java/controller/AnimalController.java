package controller;

/**
 * Az AnimalController osztály az állatok vezérléséért felelős a játékban.
 * <p>
 * Az osztály public, így más csomagokból és osztályokból is példányosítható és elérhető.
 */
public class AnimalController {
    // Az állatok vezérléséhez szükséges metódusok és attribútumok itt találhatók.
    // Például: állatok mozgásának vezérlése, állatok állapotának frissítése stb.

    /**
     * Alapértelmezett konstruktor az AnimalController osztályhoz.
     * Ez a konstruktor public, így más osztályokból is példányosítható.
     */
    public AnimalController() {
        // Inicializálás, ha szükséges
    }
}
