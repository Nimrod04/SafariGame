package controller;

/**
 * Az EntityController osztály az entitások vezérléséért felelős a játékban.
 * <p>
 * Az osztály public, így más csomagokból és osztályokból is példányosítható és elérhető.
 */
public class EntityController {
    // Az entitások vezérléséhez szükséges metódusok és attribútumok itt találhatók.

    int adsd = 6;

    /**
     * Alapértelmezett konstruktor az EntityController osztályhoz.
     * Ez a konstruktor public, így más osztályokból is példányosítható.
     */
    public EntityController() {
        // Inicializálás, ha szükséges
    }
}
